#!/usr/bin/python3
# Creator - EP

def islower(g):
    """ a function that checks for lowercase character."""
    if ord(g) >= 97 and ord(g) <= 122:
        return True
    else:
        return False
